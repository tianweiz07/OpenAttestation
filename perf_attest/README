Libvirt VM migration

Add each server ip address in file /etc/hosts.

Set up the nfs server:
server:
	apt-get install nfs-kernel-server portmap
	mkdir -p /exports/images
	chown nobody:nogroup /exports/images
	modify /etc/exports:
		/exports/images    192.168.1.0/24(rw,no_root_squash)
	exportfs -a

client:
	apt-get install nfs-common portmap
	mount nfs_server_ip:/exports/images /var/lib/libvirt/images
